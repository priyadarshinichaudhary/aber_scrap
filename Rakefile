# require "bundler/gem_tasks"
# task :default => :spec
# task :console do
#   exec "irb -r abercromscrap -I ./lib"
# end


require "bundler/gem_tasks"

require 'rspec/core/rake_task'

RSpec::Core::RakeTask.new(:spec)

task :default => :spec
